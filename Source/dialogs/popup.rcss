body {
	font-family: Gabriola;
	font-size: 48px;
	width: 100%; /* The document will use the whole display */
	height: 100%;
	color: black;
}

div {
	display: block;
}

td
{
	float: left;
}

td:first-child
{
	clear: left;
}

.header {
	font-size: 48px;
	text-align:center;
	max-height: 48px;
}

/* Used for a multi line block of text */
.content {
	height: 65%;
	white-space: pre-wrap; /* Preserve newlines, but wrap when needed */
	overflow: auto; /* Text flowing outside the area is not shown */
	line-height: 1em; /* Default is 1.2em */
	display: block;
}

/* Used for a multi line list of choices, one per line */
.menulist {
	height: 65%;
	white-space: pre-wrap; /* Preserve newlines, but wrap when needed */
	overflow: auto; /* Text flowing outside the area is not shown */
	line-height: 1em; /* Default is 1.2em */
	display: block;
	margin-left: 15px;
	margin-top: 15px;
}

/* Used for a multi line list of choices, one per line */
.controls {
	height: 65%;
	overflow: auto; /* Text flowing outside the area is not shown */
	line-height: 1em; /* Default is 1.2em */
	display: block;
	margin-left: 15px;
	margin-top: 15px;
}

/* Main formatting for dialogs */
.pergament
{
	width: 800px;
	height: 700px;
	font-size: 22px;
	background-decorator: image;
	background-image: DialogBackground.bmp;
	padding-top: 30px; /* Leave space on top of pergament background */
	padding-left: 70px;
	padding-right: 50px;
	margin-top: -370px;
	margin-left: -460px;
	z-index: -1; /* Should be behind everything else */
}

/* Use this to find the center of the screen at top left for an element */
.screencenter
{
	margin-top: 50%;
	margin-left: 50%;
	z-index: -1; /* Should be behind everything else */
}

.menuitem {
	font-size: 36px;
	display: block;
	/* max-height: 36px; */
}

.formatbutton {
	font-size: 36px;
	display: inline;
}

.firstbuttonpos {
	margin-left:100px; /* The position of the button at the bottom of dialogs. */
}

.secondbuttonpos {
	margin-left:300px; /* The position of the button at the bottom of dialogs. */
}
